/* ============================================================
   RENEW WATER — js/config.js
   Configuración global de la aplicación.
   ► EDITA SOLO ESTE ARCHIVO para cambiar la URL del webhook.
   ============================================================ */

const APP_CONFIG = {
  /**
   * URL del webhook de n8n donde se envían los formularios.
   * Reemplaza el valor de abajo con tu URL real.
   *
   * Ejemplo:
   *   webhookUrl: 'https://n8n.tudominio.com/webhook/renew-water'
   */
  webhookUrl: 'https://n8n.milian-app.online/webhook/formulario',

  /** Nombre que aparece en el título de la página */
  appName: 'RENEW WATER — Portal de Contratos',

  /** Tiempo (ms) que el toast permanece visible */
  toastDuration: 4000,
};
